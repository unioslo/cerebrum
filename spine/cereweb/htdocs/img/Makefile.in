##
## Makefile for htdocs/img/
##
SHELL = @SHELL@
INSTALL = @INSTALL@

prefix = @prefix@
libdir = @libdir@
webroot = @webroot@

sources = $(wildcard *.png)

.PHONY: all install install-lib clean distclean print

all: 

print:
	@echo Sources
	@echo $(sources)

install-lib:

install: $(sources) htaccess
	$(INSTALL) -d $(DESTDIR)/$(webroot)/img
	$(INSTALL) -v -m 644 $(sources) $(DESTDIR)/$(webroot)/img
	$(INSTALL) -v -m 644 htaccess $(DESTDIR)/$(webroot)/img/.htaccess

clean:
	rm -f *~ 

distclean: clean
	rm -f Makefile
