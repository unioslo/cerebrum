##
## Makefile for SpineClient
##

INSTALL = @INSTALL@
pythondir = @PYTHONDIR@

.PHONY: all install install-lib clean distclean

sources = $(wildcard *.py)

all: 

install: install-lib

install-lib: $(sources)
	$(INSTALL) -v -m 644 $(sources) $(pythondir)
	$(INSTALL) -v -m 644 ../lib/server/idl/SpineCore.idl $(pythondir)

clean:

distclean: clean

