$ sudo yum install postgresql-devel  # psycopg2

$ cp config.py.example config.py

$ virtualenv venv
$ . venv/bin/activate
(venv)$ pip install -r requirements.txt
(venv)$ add2virtualenv ~/cereconf/local ~/src/cerebrum /site/lib/python2.7/site-packages

$ setsebool httpd_can_network_connect 1 -P  # for nginx proxy_pass
