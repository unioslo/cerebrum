##
## Makefile for lib/classes
##
INSTALL = @INSTALL@

pythondir = @PYTHONDIR@/Cerebrum/gro/classes

sources = $(wildcard *.py)

.PHONY: all install install-lib clean distclean print

all: 

install: install-lib

install-lib: $(sources)
	@for file in $(sources); do \
	  $(INSTALL) -D -v -m 644 $$file $(DESTDIR)/$(pythondir)/$$file || exit 1; \
	done
	touch $(DESTDIR)/$(pythondir)/__init__.py
	chmod 644 $(DESTDIR)/$(pythondir)/__init__.py

clean:
	rm -f *~ 

distclean: clean
	rm -f Makefile
