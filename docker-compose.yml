version: "2"

services:
  uio:
    build:
      context: .
      dockerfile: Dockerfile-testsuite
    volumes:
      - .:/src:z
    networks:
      - cerebrum
    working_dir: /src/testsuite/docker
    depends_on:
      - db
    environment:
      - DOCKER_ROOT_DIR=/src/testsuite/docker
      - TEST_CONFIG_DIR=/src/testsuite/docker/test-config
      - CEREBRUM_CONFIG_DIR=/src/testsuite/docker/cerebrum_config
      - DB_USER=postgres
      - DB_PASS=postgres
      - DB_HOST=db
      - INST=uio
      - DB_NAME=cerebrum_uio
    command: /src/testsuite/docker/generate-test-reports.sh

  db:
    build:
      context: .
      dockerfile: Dockerfile-testsuite-db
    networks:
      - cerebrum

networks:
  cerebrum:
    driver: bridge