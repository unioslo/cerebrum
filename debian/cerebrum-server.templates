Template: cerebrum-server/db-auto-config
Type: boolean
Default: true
Description: Automatic handling of the database backend for cerebrum
 I can attempt to setup and configure the database backend (postgresql) 
 automatically for you now.
 .
 Should i attempt automatic database configuration?
