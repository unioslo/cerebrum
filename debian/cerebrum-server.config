#!/bin/sh -e


CONFOPT="db_auto_config"

# load the library
. /usr/share/debconf/confmodule

db_version 2.0

# set -x

# ask what we need to know.
db_input low cerebrum-server/db_auto_config || true
db_input low cerebrum-server/initial_accountname_password || true
db_go 

#DEBHELPER#
