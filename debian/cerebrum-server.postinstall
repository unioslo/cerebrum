# Automatically added by dh_installinit
if [ -x "/etc/init.d/cerebrum" ]; then
	update-rc.d cerebrum defaults >/dev/null
	if [ -x /usr/sbin/invoke-rc.d ]; then
		invoke-rc.d cerebrum start
	else
		/etc/init.d/cerebrum start
	fi
fi
# End automatically added section
